# Copy to ~/.config/my-opiniated-editor/bridge-7683.env.
MOE_BRIDGE_HTTP_PORT=17683
MOE_BRIDGE_HTTPS_INTERFACE=127.0.0.1

# Replace this with the exact URL printed by `tailscale funnel`, without a
# trailing slash.
# MOE_BRIDGE_ALLOWED_ORIGIN=https://nixos.example-tailnet.ts.net
