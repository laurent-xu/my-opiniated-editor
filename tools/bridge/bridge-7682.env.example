# Copy to ~/.config/my-opiniated-editor/bridge-7682.env.
MOE_BRIDGE_HTTP_PORT=17682

# This instance remains available to the LAN. Remove this override to make it
# loopback-only like the common default.
MOE_BRIDGE_HTTPS_INTERFACE=0.0.0.0
